{
    "name": "Calculate Choice Totals",
    "height": 100,
    "width": 200,
    "description": "Calculates the total count of specific archetypes (Atlas, Seer, Envoy, Nexus) across all single-choice questions",
    "parameters": {
        "targetChoice": {
            "type": "string",
            "title": "Target Choice",
            "description": "Which choice to calculate (A=Atlas, B=Seer, C=Envoy, D=Nexus)",
            "default": "A",
            "enum": ["A", "B", "C", "D"]
        }
    }
} 